# php-minecraft-sns-connect
A repository to preserve memories of scripts I made long ago

> composer使えるのか。

## require

`gh repo clone xPaw/PHP-Minecraft-Query`

## 記憶の内容
PHP rcon と PHP Query を使って、今はもう使えないけどTwitterの認証をして、  
MySQLから該当するアカウント名とMinecraftのユーザー名が合致していたら、  
データベースにあるMinecraft内の通貨Pluginの値を返す。  
管理者だった場合はRCONのコンソールを用意して遠隔で操作ができるようにというところまでは作ってたはず。  
不運にも当時GitHubを知らなくてサーバーのデータ消えたから何も残ってない。

